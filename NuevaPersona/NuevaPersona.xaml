<UserControl x:Class="Personas.NuevaPersona.NuevaPersona"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Personas.NuevaPersona"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <StackPanel Margin="10">
        <TextBlock Margin="0,10,0,0" Text="Nombre"/>
        <TextBox Text="{Binding Path=PersonaNueva.Nombre}"/>
        <TextBlock Margin="0,10,0,0" Text="Edad"/>
        <TextBox Text="{Binding Path=PersonaNueva.Edad}"/>
        <TextBlock Margin="0,10,0,0" Text="Nacionalidad"/>
        <StackPanel Orientation="Horizontal">
            <ComboBox Width="150" ItemsSource="{Binding Path=Nacionalidades}" SelectedItem="{Binding Path=Nacionalidad}"/>
            <Button Margin="5" Width="30" Command="{Binding Path=AñadirNacionalidadBtn}">+</Button>
        </StackPanel>
        <Button Width="100" HorizontalAlignment="Center" Command="{Binding Path=AceptarBtn}">Aceptar</Button>

    </StackPanel>
</UserControl>
